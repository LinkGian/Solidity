pragma solidity ^0.6.7;

contract MyFirstContract {
 
    uint256 number = 0;
    number = number + _num;
    
    function getNumberMultiplied(_num) public view returns (unit256) {
        number = _num1 * number;
    }
    
    function addNumbers(_num1, _num2) public view returns (unit256);
        number = _num1 + _num2;
 
 
    function changeNumber(uint256 _num) public {
        number = _num;
    }
 
 
    function getNumber() public view returns (uint256){
        return number;
    }
}
